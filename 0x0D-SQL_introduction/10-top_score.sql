-- Lists all records from second_table of the hbtn_0c_0 db
-- Results should display both score and the name(in this order)
-- Records should be ordered by score
SELECT score, name 
FROM second_table
ORDER BY score DESC;
